const name = "Pranjal"
const repoCount = 50

// console.log(name + repoCount + " Sett ");

// string interpolation
console.log(`Hello my name is ${name} and my repo count is ${repoCount}`);

const gameName = new String('pranjalsett')

console.log(gameName.charAt(3));
console.log(gameName.indexOf('a'));

// slice and substring functions we can use negetive values in slice but not in substring 

